# Missing Provider Registrations

**Problem:** Unknown model errors despite fallback chain.

**Solution:** Register models in models.providers.provider.models[].

**Tags:** #openclaw #providers #models